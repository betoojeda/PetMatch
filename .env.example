# =================================================
# VARIABLES DE ENTORNO PARA PETMATCH
# =================================================
# Copia este archivo a .env y rellena los valores.
# ¡NO SUBAS EL ARCHIVO .env A GIT!

# --- Base de Datos (PostgreSQL) ---
POSTGRES_DB=petmatch
POSTGRES_USER=petmatch
POSTGRES_PASSWORD=supersecretpassword123

# --- Backend (Spring Boot) ---
# Secreto para firmar los tokens JWT. Usa una cadena larga y aleatoria en producción.
JWT_SECRET=454546545646465
# Tiempo de expiración del token en milisegundos (ej. 86400000 = 24 horas)
JWT_EXPIRATION=86400000

# --- Configuración de Correo (Ejemplo con Gmail) ---
# IMPORTANTE: Para Gmail, necesitas generar una "Contraseña de aplicación".
# No uses tu contraseña de inicio de sesión normal.
SPRING_MAIL_HOST=smtp.gmail.com
SPRING_MAIL_PORT=587
SPRING_MAIL_USERNAME=tu-correo@gmail.com
SPRING_MAIL_PASSWORD=tu-contraseña-de-aplicacion

# --- URLs de la Aplicación ---
# URL del frontend para los enlaces en los correos (ej. recuperación de contraseña)
# En desarrollo: http://localhost:3000
# En producción: https://www.tu-dominio.com
FRONTEND_URL=http://localhost:3000

# URL del backend que el frontend consumirá
# En desarrollo: http://localhost:8080/api
# En producción: https://www.tu-dominio.com/api
REACT_APP_API_URL=http://localhost:8080/api
